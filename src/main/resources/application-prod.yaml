spring:
  application:
    name: freemiumwear
  profiles:
    active: prod
#  security:
#    oauth2:
#      resourceserver:
#        jwt:
#          issuer-uri: ${JWT_ISSUER_URI}
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db.migration

  datasource:
    url: jdbc:postgresql://dpg-d4g4bmogjchc73dplirg-a.oregon-postgres.render.com:5432/freemiumwear
    username: freemiumwear
    password: g4fED6TVk5gLir9vjSzhPbziwBmX3wOF
    driver-class-name: org.postgresql.Driver
    hikari:
      schema: public
      maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: public
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mail:
    from-email-id: sv2834766@gmail.com

app:
  whitelist:
    paths:
      - /v3/api-docs/**
      - /swagger-ui/**
      - /swagger-ui.html
      - /swagger-resources/**
      - /webjars/**
      - /configuration/**
      - /api/users/signUp
      - /api/login/initiate
      - /api/login/verify
      - /api/v1/login
      - /api/otp/send
      - /api/otp/verify
      - /actuator
      - /actuator/**
      - /api/login/refresh-token
      - /api/users/getUser
#
#aws:
#  region: ${AWS_REGION}
#  cognito:
#    userPoolId: ${AWS_COGNITO_USER_POOL_ID}
#    appClientId: ${AWS_COGNITO_APP_CLIENT_ID}
#  s3:
#    bucket-name: ${AWS_COMMON_S3_BUCKET}
#    bucket-folder: ${AWS_COMMON_S3_BUCKET_FOLDER}
#  sqs:
#    sms-queue-url: ${AWS_ICE_SMS_QUEUE}